# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| main（开发分支） | ✅ 安全修复会直接合入 |
| 最近一个 Release | ✅ 提供安全修复 |

更早的版本不再维护，请升级到受支持的版本。

## 报告安全漏洞

**请不要在公开 issue、Pull Request 或讨论中披露漏洞细节。**

请通过以下方式私下报告：

- 通过 GitHub 的 **Security Advisories** 功能私下提交（在仓库页面的 `Security` → `Report a vulnerability`）

### 报告时请包含

1. 漏洞类型与严重程度评估。
2. 受影响版本与修复版本（如适用）。
3. 详细复现步骤。
4. 漏洞可能造成的实际影响（例如是否可导致数据泄露、越权或服务不可用）。
5. 你推荐的修复思路（可选）。

### 响应时间

- 收到报告后，我们会尽快确认并回复，通常在 **48 小时** 内给出初步反馈。
- 确认漏洞后，我们会评估影响范围，并推进修复与发布。
- 修复发布前，不会公开披露漏洞细节，以保护其他部署者。

## 生产部署安全建议

请务必遵循 `README.md` 中「生产部署」章节的建议，至少做到：

- 使用强随机 `JWT_SECRET`，长度至少 32 个字符，且与开发环境不同。
- 修改数据库密码和所有默认演示账号密码。
- 不要把 `.env`、数据库导出、日志文件或真实密钥提交到仓库。
- 为后端配置 HTTPS 反向代理和受控的 CORS 来源。
- AI API key 只放在服务端环境变量或受控的密钥管理系统中，前端不接触。
- 通过 Security Advisories 关注本仓库及依赖的安全公告，及时升级依赖。

## 已知安全设计

- 登录使用 JWT，通过 HTTP-only Cookie 传递，前端无法用脚本读取令牌。
- AI API key 仅由后端持有，前端不读取、不保存、不打包。
- 生产 Compose 强制要求设置 `POSTGRES_PASSWORD` 和 `JWT_SECRET`，未设置会启动失败。
